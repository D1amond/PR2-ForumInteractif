{% extends "PR2ForumBundle::layout.html.twig" %}
 
{% block title %}
    Supprimer un Thème - {{ parent() }}
{% endblock %}
 
{% block pr2forum_body %}
 
    <h2>Supprimer un Thème</h2>
 
    <div class="alert alert-warning">
        Etes-vous certain de vouloir supprimer le Thème "{{ theme.nom }}" ?
    </div>
    {#{% if theme.lieux|length > 0 %}
        <div class="alert alert-danger">
            Les lieux suivants seront supprimés, et leurs sujets seront automatiquement archivés:
            <ul>
                {% for lieu in theme.lieux %}
                    <li>{{ lieu.getNom() }}</li>
                {% endfor %}S
            </ul>
        </div>
    {% endif %}#}
    {# On met l'id de la theme dans la route de l'action du formulaire #}
    <form action="{{ path('pr2_settings_theme_delete', {'id': theme.id}) }}" method="post">
        <a href="{{ path('pr2_settings_theme') }}" class="btn btn-default">
            <span class="glyphicon glyphicon-chevron-left"></span>
            Retour à l'index
        </a>
        <input type="submit" value="Supprimer" class="btn btn-danger" />
        {{ form_rest(form) }}
    </form>
 
{% endblock %}