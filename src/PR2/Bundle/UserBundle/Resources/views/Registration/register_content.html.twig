{% trans_default_domain 'PR2UserBundle' %}

<form action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register">
    {{ form_widget(form) }}
    <div>
        <button type="submit" value="">{{ 'registration.submit'|trans }}</button>
    </div>
</form>
